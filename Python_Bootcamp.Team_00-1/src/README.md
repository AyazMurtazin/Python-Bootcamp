Установка окружения: `python3 -m venv venv`

Активация окружения: `source venv/bin/activate`

Установка пакетов в локальном окружении: `pip install -r requirements.txt`

Выход из локального окружения: `deactivate`